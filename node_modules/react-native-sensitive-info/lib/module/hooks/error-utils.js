"use strict";

import { isAuthenticationCanceledError as internalIsAuthenticationCanceledError } from "../errors.js";
import { getErrorMessage } from "../internal/errors.js";
import { HookError } from "./types.js";

/**
 * Creates a {@link HookError} enhanced with the original cause, the operation label, and an optional hint.
 */
const createHookError = (operation, error, hint) => new HookError(`${operation}: ${internalIsAuthenticationCanceledError(error) ? 'Authentication prompt canceled by the user.' : getErrorMessage(error)}`, {
  cause: error,
  operation,
  hint
});
export const isAuthenticationCanceledError = internalIsAuthenticationCanceledError;
export default createHookError;
//# sourceMappingURL=error-utils.js.map